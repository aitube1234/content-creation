# 

## Setup

1. Copy `.env.example` to `.env` and configure
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `uvicorn backend.services.content_creation.main:app --reload`

## Project Structure

See `docs/` for architecture documentation.
